Stub — no `agent:implement` phase instructions found in the target repo's own
`docs/agent-phases/implement.md`.

Use judgment: implement the agreed plan, open a PR against the default
branch. End with the usual trailer line(s) the calling repo's trigger
workflow asked for (`AGENT_PR: <pr>` then `AGENT_OUTCOME: READY`, or
`AGENT_OUTCOME: BLOCKED` if no PR was opened).
