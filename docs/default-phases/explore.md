Stub — no `agent:explore` phase instructions found in the target repo's own
`docs/agent-phases/explore.md`.

Use judgment: explore the issue and the codebase enough to say whether it's
ready to design/implement, or blocked on missing information. End with the
usual trailer line(s) the calling repo's trigger workflow asked for
(`AGENT_OUTCOME: READY` or `AGENT_OUTCOME: BLOCKED`).
