Stub — no `agent:design` phase instructions found in the target repo's own
`docs/agent-phases/design.md`.

Use judgment: turn the issue into an implementation plan, posted as a comment.
End with the usual trailer line(s) the calling repo's trigger workflow asked
for (`AGENT_OUTCOME: READY` or `AGENT_OUTCOME: BLOCKED`).
